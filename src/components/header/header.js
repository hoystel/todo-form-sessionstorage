import React from 'react';
import './header.css';

const Header = () => {
    // didnt need render as im just returning something to render elsewhere
        return(
            <div className="header">
                <h4>Form based application</h4>
            </div>   
        )
    };

export default Header;